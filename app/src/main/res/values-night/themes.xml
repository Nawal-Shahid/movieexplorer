<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Dark theme variants -->
    <style name="Base.Theme.MovieExplorer" parent="Theme.Material3.DayNight">
        <!-- Dark theme customization -->
        <item name="android:forceDarkAllowed">true</item>
        <item name="android:statusBarColor">@color/primary_container</item>
        <item name="android:navigationBarColor">@color/background</item>
    </style>

    <!-- Dark splash screen theme -->
    <style name="Theme.MovieExplorer.Splash" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/primary_container</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>